# .NET
[Bb]in/
[Oo]bj/
TestResults/
*.user

# Node
node_modules/
dist/
.next/
coverage/

# Prisma: o cliente e gerado por `pnpm prisma generate` (roda no postinstall)
frontend/generated/

# Ambiente
.env
.env.local
!.env.example

# Documentos de trabalho, nao de entrega.
#
# O HANDOFF e escrito para quem (ou o que) for retomar o codigo, e o briefing de
# design para uma ferramenta de design - os dois falam de processo, nao do
# sistema. Quem avalia le a APRESENTACAO, e e la que as decisoes e as armadilhas
# que importam estao registradas.
docs/HANDOFF.md
docs/BRIEFING-DESIGN.md

# Diversos
*.log
.DS_Store
.vs/
.idea/

# Cache incremental do TypeScript: artefato de build, muda a cada `tsc`.
*.tsbuildinfo

# Skills de agente instaladas por integracoes de marketplace. Ferramental de
# quem desenvolve, nao do projeto.
.claude/
.agents/
